import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    root: {
      maxWidth: 400,
      height: 450,
      flexGrow: 1,
      marginLeft: 30,
      position: 'relative',
    },
  }));